{
  "extends": "@tsconfig/node18/tsconfig.json",
  "compilerOptions": {
    "lib": ["es2023", "dom"],
    "strict": true
  },
  "include": ["src/**/*"],
  "exclude": ["**/*.spec.ts"],
  "rootDir": "./src",
  "outDir": "./dist"  /* TODO: Fix ->>> $tsc --outDir dist works but $tsc doesn't work with this config*/
}